name: Deploy CloudOps App

on:
  push:
    branches:
      - main
      - feature/cloudwatch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do Código
        uses: actions/checkout@v3

      - name: Configurar Credenciais AWS
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Configurar Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Deploy Backend (Terraform)
        run: |
          cd backend
          terraform init
          terraform apply -auto-approve

      # 🚀 NOVO PASSO: Exibe os Outputs no Log e cria o Summary na UI
      - name: Exibir Outputs do Terraform
        run: |
          cd backend
          echo "### 🚀 Outputs do Deploy" >> $GITHUB_STEP_SUMMARY
          echo '```hcl' >> $GITHUB_STEP_SUMMARY
          terraform output >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          
          # Exibe também direto no console do log
          terraform output

      - name: Deploy Frontend (S3)
        run: |
          cd frontend
          aws s3 sync . s3://aws-cloudops-app-frontend --delete
